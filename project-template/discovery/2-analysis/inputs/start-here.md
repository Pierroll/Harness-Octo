# Empieza aquí — etapa 2

Abre esta carpeta cuando el análisis **empieza ahora**, hayas corrido o no la etapa 1.

## 1. Transcripts (siempre)

Déjalos en **esta misma carpeta**, no en `inherited/`:

`d1-transcript.md`, `d2-transcript.md`, … (los trae `import-transcript` o un pegado a mano).

Sin transcript no hay sesión procesable. Estos son de d1, d2, d3: no los de venta.

## 2. Lo que la etapa 1 ya debería haber producido

**Si corriste la etapa 1 en este repo:** no toques `inherited/`. Esos archivos ya viven
en `discovery/1-planning/outputs/` y en `backlog.md`.

**Si entras directo a la etapa 2:** deja el paquete en `inherited/` (nombres fijos,
lista en `inherited/README.md`) y corre `harness/skills/entry-check/` **antes** de
procesar transcripts.

## 3. Qué decirle al agente

> Abre la etapa 2. La modalidad está en `overrides.md`. Corre `entry-check`.
> Los transcripts están en `discovery/2-analysis/inputs/`. El paquete heredado,
> si lo hay, está en `inputs/inherited/`. No corras la etapa 1 completa.

El veredicto es entra / entra con huecos / no entra.
